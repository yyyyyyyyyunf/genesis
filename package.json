{
  "name": "hercules-lowcode-platform",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "scripts": {
    "dev:hercules": "pnpm --filter hercules dev",
    "dev:zeus": "pnpm --filter zeus dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
